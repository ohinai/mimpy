language: python
python:
    - "2.7"
    
env:
- TOX_ENV=py27
- TOX_ENV=py33
- TOX_ENV=py34

install: 
    - pip install --install-option="--no-cython-compile" Cython
    - pip install tox
    - pip install numpy 

script:
    - python setup.py install
    - tox -e $TOX_ENV
